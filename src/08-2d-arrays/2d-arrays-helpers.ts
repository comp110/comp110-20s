/**
 * Given the number of rows and columns initialize a new 2d array of that size
 * filled with 0s.
 */
export let array2d = (rows: number, cols: number): number[][] => {
    let a: number[][] = [];

    // TODO

    return a;
};

/**
 * Given the number of rows and columns, initialize a new 2d array of that size
 * filled with the multiplication of row+1 * col+1. For example:
 * 
 * Row| 0 1 2 <- Col
 * -----------
 *  0 | 1 2 3
 *  1 | 2 4 6
 *  2 | 3 6 9
 *  3 | 4 8 12
 */
export let multiply2d = (rows: number, cols: number): number[][] => {
    let a: number[][] = [];

    // TODO

    return a;
};