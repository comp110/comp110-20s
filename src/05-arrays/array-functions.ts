
// TODO: move definition of sum here

/**
 * Given an array of numbers, return a string of all numbers
 * concatenated together and separated by `delimiter`. Ex:
 * if a is [1, 2, 3] and delimiter is "-" the returned string would be:
 * "1-2-3"
 */
export let join = (a: number[], delimiter: string): string => {
    return "";
};

/**
 * Fill an array of numbers from low to high, inclusive.
 */
export let fillRange = (low: number, high: number): number[] => {
    return [];
};